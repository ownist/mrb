/**
 * ৬. Function (ফাংশন)
 *
 * টাস্ক: এমন একটি ফাংশন লিখুন যা দুটি সংখ্যা ইনপুট (parameter) হিসেবে নিবে এবং তাদের গুণফল রিটার্ন করবে। ফাংশনটি কল করে ফলাফল কনসোলে দেখান।
 *
 * Commit Message: feat: implement multiplication function
 */

function multiply(x, y) {
  return x * y;
}

const firstMultiplyValue = 2;
const secondMultiplyValue = 5;
const result = multiply(firstMultiplyValue, secondMultiplyValue);
console.log(result);
