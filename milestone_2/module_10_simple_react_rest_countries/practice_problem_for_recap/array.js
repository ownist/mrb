/**
 * ৪. Array (অ্যারে)
 *
 * টাস্ক: ৫টি ফলের নাম দিয়ে একটি অ্যারে তৈরি করুন। লুপ ব্যবহার করে অ্যারের প্রতিটি ফলের নাম আলাদা আলাদা ভাবে প্রিন্ট করুন।
 *
 * Commit Message: feat: create fruit array and iterate through items
 */

const fol = ["am", "jam", "lichu", "kola", "kathal"];

for (let index = 0; index < fol.length; index++) {
  console.log(fol[index]);
}
