const poem = `amr sonar bangla
ai tomai valobashi
chirodin tomer akash`;
console.log(poem);

function sum(num1, num2) {
  return `sum of ${num1} and ${num2} is equal to: ${num1 + num2}`;
}
console.log(sum(2, 5));

const price = 100;
let discountPrice = (price * 20) / 100;
const result = `discount price is ${price - discountPrice}`;
console.log(result);
